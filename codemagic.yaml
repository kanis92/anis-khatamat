# Codemagic - Configuration ANIS Khatamat
# Documentation: https://docs.codemagic.io/flutter-configuration/yaml

workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 90
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.aniskhatamat.app
    scripts:
      - name: Flutter pub get
        script: flutter pub get
      - name: Clean iOS Pods (fresh install for gRPC fix)
        script: |
          cd ios && rm -rf Pods Podfile.lock .symlinks && cd ..
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Flutter build IPA
        script: flutter build ipa
    artifacts:
      - build/ios/ipa/*.ipa
      - build/ios/archive/*.xcarchive
      - build/ios/iphoneos/*.app
    publishing:
      scripts:
        - name: Build info
          script: |
            echo "Build terminé avec succès"
            flutter --version
